#pragma once

namespace Glim {

	enum class Corner { TopLeft = 0, BottomLeft = 1, BottomRight = 2, TopRight = 3 };
	enum class Alignment { Left = 0, Center = 1, Right = 2 };
	enum class Orientation { Horizontal = 0, Vertical = 1 };
}