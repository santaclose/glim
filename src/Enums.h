#pragma once

namespace Glim {

	enum class Corner { TopLeft = 0, BottomLeft = 1, BottomRight = 2, TopRight = 3 };
	enum class HAlignment { Left = 0, Center = 1, Right = 2 };
	enum class VAlignment { Top = 0, Center = 1, Bottom = 2 };
	enum class Orientation { Horizontal = 0, Vertical = 1 };
}